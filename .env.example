# QuantAI 环境配置模板 — 复制为 .env 并填入真实值（.env 不入库）
# 账密（真源 autotrade_fix.py L99-100 硬编码，本版改 .env）
QUANTAI_ACCOUNT=
QUANTAI_PASSWORD=

# 运行模式: 1 = dry_run（不下单/撤单，独立 sim 账户或只读连接）
QUANTAI_DRY_RUN=0

# 数据目录（可选，默认 <项目根>/data）
# QUANTAI_DATA_DIR=
